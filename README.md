a complate-specific wrapper for [faucet-pipeline](http://www.faucet-pipeline.org)


Getting Started
---------------

* ensure [Node](http://nodejs.org) is installed
* `npm install` downloads dependencies
* `npm test` checks code for stylistic consistency
